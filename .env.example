# Preferred provider routing:
# 1) If OPENAI_API_KEY is set, OpenAI + GPT model is used.
# 2) Else if ANTHROPIC_API_KEY is set, Anthropic model is used.
# 3) AGENT_MODEL can explicitly override model/provider.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional explicit override. Examples:
# AGENT_MODEL=openai/gpt-4.1-mini
# AGENT_MODEL=anthropic/claude-3-5-sonnet-latest
AGENT_MODEL=
